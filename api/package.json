{
  "name": "api",
  "version": "1.0.0",
  "description": "api",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../ && npm run build && cp -r dist ../api",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "build": "npm i",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kobrak1/fs-open-from-part3.git"
  },
  "author": "burak karhan",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.0.3",
    "morgan": "^1.10.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
